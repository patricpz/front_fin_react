# URL base da API (FastAPI) — inclui o prefixo /api/v1
VITE_API_URL=http://localhost:8000/api/v1

# Origem da API (usada para health check em /health)
VITE_API_ORIGIN=http://localhost:8000

# Use "true" para rodar com dados mock sem backend; "false" para consumir a API real
VITE_USE_MOCK=false
